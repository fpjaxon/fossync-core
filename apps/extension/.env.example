# AMO API credentials for unlisted signing.
# Get them at https://addons.mozilla.org/developers/addon/api/key/
# Export these (or copy to apps/extension/.env and source it) before `pnpm release`.
AMO_JWT_ISSUER=
AMO_JWT_SECRET=
